# Runtime dependencies for the ORACLE chatbot child agent.
# The core agent uses only the Python standard library, so no packages are
# required unless an optional LLM backend (e.g. openai, anthropic) is used.

# Optional: required only for OpenAIBackend
# openai>=1.0.0
